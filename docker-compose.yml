version: "3.8"

services:
  db:
    image: postgres:latest
    container_name: my-postgres-db
    env_file:
      - .env.development
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always

volumes:
  db_data:
